topic_synchronizer:
  ros__parameters:
    topics:
      source:
        odom_filtered: /locomotion/odom_filtered
        odom_gt: /zed_camera/odom_transf
      output:
        odom_filtered: /emrs/odometry/filtered_transf
        odom_gt: /gnss/odom
    interval_duration: 500000000.0
    approx_policy: 10